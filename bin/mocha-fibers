#!/usr/bin/env node

var spawn = require('child_process').spawn
var child = spawn("./node_modules/.bin/mocha", ["--require", "./lib/mocha-fibers", "--ui", "fibered"].concat(process.argv.slice(2)), {stdio: [0,1,2]});

